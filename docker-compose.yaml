services:
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - "6379:6379"
  app:
    build: .
    container_name: backend
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      - redis
