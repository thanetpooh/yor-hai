{
  "name": "yor-hai",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,ui -c cyan,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vitest": "^4.1.7"
  }
}
